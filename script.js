const API_URL="https://SEU-BACKEND.onrender.com";const btn=document.querySelector("#generate"),status=document.querySelector("#status"),results=document.querySelector("#results");btn.onclick=async()=>{const url=document.querySelector("#url").value.trim(),amount=+document.querySelector("#amount").value;if(!url){status.textContent="Cole um link do YouTube.";return}btn.disabled=true;results.innerHTML="";status.textContent="Iniciando...";try{const r=await fetch(API_URL+"/jobs",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url,amount})});if(!r.ok)throw Error("Não foi possível iniciar.");const job=await r.json();const timer=setInterval(async()=>{try{const x=await fetch(API_URL+"/jobs/"+job.id);const d=await x.json();status.textContent=d.message||"Processando...";if(d.status==="done"){clearInterval(timer);status.textContent="Cortes prontos!";results.innerHTML=(d.clips||[]).map((c,i)=>`<div class="clip"><h3>🎬 Corte ${i+1}: ${esc(c.title||"Corte")}</h3><p>Score viral: ${c.viral_score??"-"}/100</p><p>${esc(c.reason||"")}</p><a href="${c.url}" target="_blank">Abrir corte</a></div>`).join("");btn.disabled=false}if(d.status==="error"){clearInterval(timer);status.textContent="Erro: "+d.message;btn.disabled=false}}catch(e){clearInterval(timer);status.textContent="Erro ao consultar servidor.";btn.disabled=false}},2500)}catch(e){status.textContent=e.message;btn.disabled=false}};function esc(s){return String(s).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;")}
